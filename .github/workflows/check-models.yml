name: Check Items

on:
  schedule:
    - cron: '*/30 * * * *'  # every 30 minutes
  workflow_dispatch:          # allow manual runs from the Actions tab

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Run checker
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          IGDB_CLIENT_ID: ${{ secrets.IGDB_CLIENT_ID }}
          IGDB_CLIENT_SECRET: ${{ secrets.IGDB_CLIENT_SECRET }}
          PUSH_API_URL: ${{ secrets.PUSH_API_URL }}
          PUSH_SEND_TOKEN: ${{ secrets.PUSH_SEND_TOKEN }}
          DATA_JSON_PATH: ${{ github.workspace }}/data.json
          COMMIT_MSG_PATH: ${{ runner.temp }}/commit-msg.txt
        run: gradle -p checker run

      - name: Commit updated data.json
        env:
          COMMIT_MSG_PATH: ${{ runner.temp }}/commit-msg.txt
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          if git diff --staged --quiet; then
            echo "No item changes — leaving the 'updated' timestamp where it is."
          else
            if [ -f "$COMMIT_MSG_PATH" ]; then
              git commit -F "$COMMIT_MSG_PATH"
            else
              git commit -m "chore: update item status"
            fi
            git push
          fi
